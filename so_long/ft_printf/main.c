#include"ft_printf.h"
#include"ft_printf.c"
#include"ft_printf_utils.c"
#include<stdio.h>
#include<limits.h>

int main (void)
{
    // char			c = 'k';
    // char			s[] = "Hello";
    // int				nb = INT_MAX;
    // unsigned int    nbr = INT_MAX;
	// char			*p = "4587";
	// unsigned int	h = UINT_MAX;
	// unsigned int	x = UINT_MAX;

	// int i = ft_printf("%c	%s	%d	%u	%p	%x	%X \n", c, s, nb, nbr, p, h, x);
	// int j = printf("%c	%s	%d	%u	%p	%x	%X \n", c, s, nb, nbr, p, h, x);
	// printf("%d - %d\n", i, j);
	//int a;
	ft_printf("%d -> %u", &a, &a);
	// printf("%d", &a);

}